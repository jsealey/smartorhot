<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
<head>
	<meta charset="utf-8" />
	
	<!-- Uncomment to make IE8 render like IE7 -->
	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->
	
	<!-- Set the viewport width to device width for mobile -->
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	
	<title><%= content_for?(:title) ? yield(:title) : "Smart or Hot" %></title>
  
	<!-- Included CSS Files -->
	<%= stylesheet_link_tag    "application" %>
	
	<!-- Included JS Files  -->
	<%= javascript_include_tag "application" %>
  
  <%= csrf_meta_tags %>
</head>
<body>

  <!-- Basic Needs -->
  <div class="row">
  <div class="twelve columns">
    <nav class="top-bar">
      <ul>
        <!-- Title Area -->
        <li class="name" style="background-color: orange !important;">
              <%= link_to image_tag("soh.svg", :size => '52x52'), root_path(), :class => 'logo' %>
        </li>
        <li class="toggle-topbar"><a href="#"></a></li>
      </ul>

      <section>
        <!-- Left Nav Section -->
        <ul class="left">
          <li class="divider"></li>
          <% if user_signed_in? %>
          <%= nav_link 'Vote', root_path %>
          <% else %>
          <%= nav_link 'Home', root_path %>
          <% end %>
          <%= nav_link "Highest Rated", attractive_path %>
          <li class="divider hide-for-small"></li>
        </ul>

        <!-- Right Nav Section -->
        <ul class="right">
          <li class="divider show-for-medium-and-up"></li>
          <% if user_signed_in? %>
          <li class="has-dropdown">
            <%= link_to 'My Account', edit_user_registration_path %>
            <ul class="dropdown">
              <%= nav_link 'Edit', edit_user_registration_path %>
              <li><%= link_to 'Sign out', destroy_user_session_path, :method => :delete %></li>
            </ul>
          </li>
          <% else %>
          <%= nav_link 'Sign In', user_session_path %>
          <% end %>
        </ul>
      </section>
    </nav>
  </div>
</div>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <article class="content">
    <%= yield %>
  </article>
</body>
</html>